import React from "react";

const App = () => {
	return (
		<div style={{
			display: "flex",
			alignItems: "center",
			justifyContent: 'center',
			height: "100vh"
		}}>
			<div style={{height: 600, width: 600, backgroundColor: '#cce3be'}}></div>
		</div>

	);
};

export default App;
